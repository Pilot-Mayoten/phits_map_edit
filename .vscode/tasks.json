{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run PHITS Map Editor",
            "type": "shell",
            "command": "python",
            "args": ["main.py"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
