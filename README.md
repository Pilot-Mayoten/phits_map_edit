# PHITS Map Editor & Route Planner

PHITSを用いた放射線環境下でのロボット等の移動シミュレーションを支援する統合GUIアプリケーションです。

## 主な特徴

- 直感的なGUIでマップ（壁・線源・スタート・ゴール等）を作成
- マップの保存・読み込み（JSON形式）
- PHITSによる線量マップ自動生成・読込
- A\*アルゴリズムによる最適経路探索（被ばく回避重み付け可）
- 複数経路の管理・比較（各経路ごとに重み・ステップ幅・合計線量を表示）
- 経路ごとの詳細線量評価ファイル一括生成
- PHITS一括実行・経路ごとの積算線量プロファイル自動グラフ化
- 2D経路・障害物・線源の可視化（マップ縮尺に忠実なグリッド表示）
- 結果のCSV出力・Excel連携

## 必要環境

- Windows OS
- Python 3.10 以上（推奨: 3.13）
- PHITS（別途インストールし、`phits.bat`をPATHに追加）
- Pythonライブラリ: `tkinter`（標準）、`matplotlib`、`numpy`

## セットアップ

1. プロジェクト直下で仮想環境を作成し、必要なライブラリをインストール

    ```powershell
    python -m venv .venv
    .\.venv\Scripts\Activate.ps1
    python -m pip install --upgrade pip
    pip install matplotlib numpy
    ```

2. PHITSの`phits.bat`がPATHに通っていることを確認

## 起動方法

- PowerShell: `./run.ps1`
- コマンドプロンプト: `run.bat`

> これらのスクリプトはTcl/Tkのパスを自動設定し、仮想環境のPythonで`main.py`を実行します。

## 使い方ワークフロー

1. **マップ作成**: GUIで壁・線源・スタート・ゴール等を配置。必要に応じて「マップを保存」「マップを読込」
2. **環境入力生成・線量マップ作成**: 「1. 環境入力を生成」→「続けてPHITSシミュレーション」→「2. 線量マップ読込」
3. **経路追加・最適化**: 「経路を追加」→リストから選択→「3. 最適経路を探索」
4. **詳細評価・結果確認**: 「4. 経路上の詳細線量評価」→「5. PHITS実行と結果プロット」
5. **プロファイル・2D表示・CSV出力**: 経路選択→「線量プロファイルを表示」「経路を2D表示」「結果をExcelで開く」

---

## システムフローチャート

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              アプリケーション起動                             │
│                            python main.py (run.ps1)                         │
└─────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          MainApplication (main.py)                          │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  ・config.ini読込 (ConfigManager)                                    │   │
│  │  ・GUIウィンドウ初期化                                                │   │
│  │  ・各モジュール統合・イベント接続                                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                        │
           ┌────────────────────────────┼────────────────────────────┐
           ▼                            ▼                            ▼
┌──────────────────────┐   ┌──────────────────────┐   ┌──────────────────────┐
│  MapEditorView       │   │SimulationControlsView│   │    ログ表示エリア     │
│  (map_editor_view.py)│   │(simulation_controls_ │   │  (ScrolledText)      │
│                      │   │      view.py)        │   │                      │
│ ・ツールボックス      │   │ ・経路管理パネル     │   │ ・実行状況表示        │
│ ・マップグリッド描画  │   │ ・実行ボタン群       │   │ ・エラー・警告表示    │
│ ・セル編集機能        │   │ ・経路リスト(Tree)   │   │                      │
│ ・マップ保存/読込     │   │ ・結果アクション     │   │                      │
└──────────────────────┘   └──────────────────────┘   └──────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            ユーザー操作フロー                                 │
└─────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
 STEP 1: マップ作成
═══════════════════════════════════════════════════════════════════════════════
    ユーザー操作                          処理フロー
    ─────────────                    ─────────────────
    [グリッドをクリック]  ───────────▶  on_cell_click()
           │                                │
           ▼                                ▼
    壁/線源/スタート/                   map_data[][] 更新
    ゴール等を配置                          │
           │                                ▼
           ▼                          グリッド表示更新
    [マップを保存/読込]  ───────────▶  save_map_dialog() / load_map_dialog()
                                            │
                                            ▼
                                       JSON形式で保存/読込

═══════════════════════════════════════════════════════════════════════════════
 STEP 2: 環境入力生成 → PHITS実行 → 線量マップ読込
═══════════════════════════════════════════════════════════════════════════════
    ユーザー操作                          処理フロー
    ─────────────                    ─────────────────
    [1.環境入力を生成]  ─────────────▶  generate_env_map()
           │                                │
           │                                ├─▶ 核種/放射能入力ダイアログ
           │                                │
           │                                ▼
           │                          phits_handler.py
           │                    generate_environment_input_file()
           │                                │
           │                                ▼
           │                          env_input.inp 生成
           │                                │
           ▼                                ▼
    [PHITSを実行?]  ────────YES────▶  run_env_simulation_threaded()
           │                                │
           │                                ▼
           │                          phits.bat 実行 (別スレッド)
           │                                │
           │                                ▼
           │                          deposit_xy.out 生成
           ▼
    [2.線量マップ読込]  ─────────────▶  load_dose_map()
                                            │
                                            ▼
                                      phits_handler.py
                                  load_and_parse_dose_map()
                                            │
                                            ▼
                                      dose_map[][] 格納
                                            │
                                            ▼
                                      visualizer.py で可視化

═══════════════════════════════════════════════════════════════════════════════
 STEP 3: 経路追加 → 最適経路探索
═══════════════════════════════════════════════════════════════════════════════
    ユーザー操作                          処理フロー
    ─────────────                    ─────────────────
    [経路を追加]  ───────────────────▶  add_route()
           │                                │
           │                                ├─▶ 重み(ω)/ステップ幅入力
           │                                │
           │                                ▼
           │                          routes[] に追加
           │                                │
           ▼                                ▼
    [3.最適経路を探索]  ─────────────▶  calculate_optimal_route()
                                            │
                                            ▼
                                      route_calculator.py
                                  find_optimal_route(start, goal, ...)
                                            │
                                            ▼
                                      run_astar() - A*アルゴリズム
                                            │
                                            ├─▶ コスト計算: 距離 + ω × 線量
                                            ├─▶ ヒューリスティック: ユークリッド距離
                                            │
                                            ▼
                                      最適経路のパスリスト
                                            │
                                            ▼
                                      グリッド上に経路描画

═══════════════════════════════════════════════════════════════════════════════
 STEP 4: 詳細線量評価ファイル生成
═══════════════════════════════════════════════════════════════════════════════
    ユーザー操作                          処理フロー
    ─────────────                    ─────────────────
    [4.経路上の詳細     ─────────────▶  run_detailed_simulation()
       線量評価]                            │
           │                                ├─▶ maxcas/maxbch 入力
           │                                ├─▶ 保存先フォルダ選択
           │                                │
           │                                ▼
           │                          phits_handler.py
           │              generate_detailed_simulation_files()
           │                                │
           │                                ├─▶ template.inp 読込
           │                                ├─▶ env_input.inp とマージ
           │                                ├─▶ AdvancedPhitsMerger でID調整
           │                                │
           │                                ▼
           │                          route_X/
           │                            ├── detailed_point_0001.inp
           │                            ├── detailed_point_0002.inp
           │                            └── ...
           ▼

═══════════════════════════════════════════════════════════════════════════════
 STEP 5: PHITS一括実行 → 結果プロット
═══════════════════════════════════════════════════════════════════════════════
    ユーザー操作                          処理フロー
    ─────────────                    ─────────────────
    [5.PHITS実行と      ─────────────▶  run_phits_and_plot_threaded()
       結果プロット]                        │
           │                                ▼
           │                    run_phits_and_plot_worker() (別スレッド)
           │                                │
           │                                ├─▶ 各 route_X/ フォルダを巡回
           │                                │      │
           │                                │      ▼
           │                                │   各 detailed_*.inp に対して
           │                                │   execute_phits_simulation()
           │                                │      │
           │                                │      ▼
           │                                │   phits.bat 実行
           │                                │      │
           │                                │      ▼
           │                                │   deposit.out 生成
           │                                │
           │                                ▼
           │                    extract_dose_from_deposit() で解析
           │                                │
           │                                ▼
           │                          result_queue に結果送信
           │                                │
           ▼                                ▼
      (結果待ち)  ◀────────────────   process_result_queue()
           │                                │
           │                                ├─▶ 経路ごとの積算線量計算
           │                                ├─▶ routes[] に total_dose 格納
           │                                ├─▶ ツリービュー更新
           │                                │
           │                                ▼
           │                          visualizer.py
           │                    plot_dose_profile(results, routes)
           │                                │
           ▼                                ▼
    [線量プロファイルを表示]             グラフ表示
    [結果をExcelで開く]  ──────────▶  results_exporter.py
                                    generate_results_csv()
                                            │
                                            ▼
                                      CSV出力 → Excel起動

═══════════════════════════════════════════════════════════════════════════════
```

### モジュール構成図

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              main.py (メインコントローラ)                     │
│                                                                             │
│   ・アプリケーションエントリーポイント                                         │
│   ・全モジュールの統合・イベントハンドリング                                    │
│   ・スレッド管理 (PHITSバックグラウンド実行)                                   │
└─────────────────────────────────────────────────────────────────────────────┘
        │              │               │              │              │
        ▼              ▼               ▼              ▼              ▼
┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐
│config_     │ │map_editor_ │ │simulation_ │ │phits_      │ │route_      │
│loader.py   │ │view.py     │ │controls_   │ │handler.py  │ │calculator  │
│            │ │            │ │view.py     │ │            │ │.py         │
├────────────┤ ├────────────┤ ├────────────┤ ├────────────┤ ├────────────┤
│・設定読込   │ │・マップ編集 │ │・ボタンUI  │ │・PHITS入力 │ │・A*探索    │
│・config.ini│ │・グリッド   │ │・経路リスト│ │  ファイル  │ │・コスト    │
│            │ │・ツール選択 │ │・ログ表示  │ │  生成      │ │  計算      │
└────────────┘ └────────────┘ └────────────┘ │・PHITS実行 │ │・経路最適化│
                                             │・結果解析  │ └────────────┘
                                             └────────────┘
        │              │               │              │
        ▼              ▼               ▼              ▼
┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐
│app_config  │ │visualizer  │ │results_    │ │utils.py    │
│.py         │ │.py         │ │exporter.py │ │            │
├────────────┤ ├────────────┤ ├────────────┤ ├────────────┤
│・定数定義   │ │・matplotlib│ │・CSV出力   │ │・座標変換  │
│・グリッド   │ │  描画      │ │・Excel連携 │ │・共通関数  │
│  設定      │ │・線量マップ │ │            │ │            │
│・セル種別  │ │・プロファイル│ │            │ │            │
└────────────┘ └────────────┘ └────────────┘ └────────────┘

        ┌────────────────────────────────────┐
        │           外部ファイル               │
        ├────────────────────────────────────┤
        │  config.ini    - アプリ設定         │
        │  template.inp  - PHITS詳細評価用    │
        │  *.json        - マップ保存データ   │
        └────────────────────────────────────┘
```

### データフロー概念図

```
    [マップデータ]                    [線量マップ]                 [経路データ]
         │                               │                            │
         │     map_data[][]              │    dose_map[][]            │   routes[]
         │                               │                            │
         ▼                               ▼                            ▼
┌─────────────┐                 ┌─────────────┐              ┌─────────────┐
│ 壁: 1       │                 │ 線量値[Gy]  │              │ weight: ω   │
│ 床: 0       │ ───────────────▶│ グリッド    │ ────────────▶│ step: cm    │
│ 線源: 2     │                 │ 各セル値    │              │ path: [...] │
│ スタート: 3 │                 └─────────────┘              │ color: ...  │
│ ゴール: 4   │                       │                      │ total_dose  │
└─────────────┘                       │                      └─────────────┘
      │                               │                            │
      │                               ▼                            │
      │                    ┌─────────────────────┐                 │
      │                    │   A* 経路探索        │                 │
      └───────────────────▶│                     │◀────────────────┘
                           │ cost = dist + ω×dose│
                           └─────────────────────┘
                                     │
                                     ▼
                           ┌─────────────────────┐
                           │   詳細線量評価       │
                           │   (PHITS一括実行)    │
                           └─────────────────────┘
                                     │
                                     ▼
                           ┌─────────────────────┐
                           │   結果出力           │
                           │  ・グラフ表示        │
                           │  ・CSV/Excel出力     │
                           └─────────────────────┘
```

## 設定ファイル（config.ini）

アプリ全体のUIサイズやPHITSコマンド、フォント設定などを`config.ini`で管理できます。

```ini
[UI]
window_width = 1600
window_height = 1080
grid_width = 1050
control_panel_width = 300

[PHITS]
command = phits.bat
default_maxcas = 10000
default_maxbch = 10

[Visualization]
font_directory = C:/Windows/Fonts
font_files = meiryo.ttc,msgothic.ttc,yugothb.ttc
```

## テンプレートファイル（template.inp）

詳細評価用PHITS入力ファイルのテンプレート。ロボット形状や検出器設定を編集可能。

---

ご質問・不具合報告はGitHubのIssueまたはリポジトリ管理者まで。
